(()=>{'use strict';window.addEventListener('DOMContentLoaded',()=>{const filters=document.querySelector('#filters');if(!filters)return;const labels=[['all','Všech 35'],['live','LIVE'],['api','API DEMO'],['demo','DEMO'],['architecture','ARCHITEKTURA']];filters.innerHTML=labels.map((x,i)=>`<button class="course-filter ${i===0?'active':''}" data-filter="${x[0]}">${x[1]}</button>`).join('');filters.addEventListener('click',e=>{const b=e.target.closest('.course-filter');if(!b)return;filters.querySelectorAll('button').forEach(x=>x.classList.remove('active'));b.classList.add('active');document.querySelectorAll('.lesson').forEach(c=>{const pill=c.querySelector('.pill:nth-of-type(2)')?.textContent?.trim();c.hidden=b.dataset.filter!=='all'&&((b.dataset.filter==='live'&&pill!=='LIVE')||(b.dataset.filter==='api'&&pill!=='API DEMO')||(b.dataset.filter==='demo'&&pill!=='DEMO')||(b.dataset.filter==='architecture'&&pill!=='ARCHITEKTURA'))})});const reset=document.createElement('button');reset.className='course-filter reset';reset.textContent='↺ Začít znovu';reset.onclick=()=>{Object.keys(localStorage).filter(k=>k.startsWith('course-done-')).forEach(k=>localStorage.removeItem(k));document.querySelectorAll('.lesson').forEach(c=>c.classList.remove('learned'));document.querySelector('#course-progress').textContent='0 / 35';document.querySelector('#course-bar').style.width='0%';document.querySelectorAll('.demo-result').forEach(x=>x.hidden=true)};filters.appendChild(reset);});})();